# AdaOS Drive Scenario

This scenario opens the AdaOS Drive browser surface. The implementation lives in
the `adaos_drive` skill so the same two-panel file manager can be launched from
the desktop app tile or directly from this scenario.

