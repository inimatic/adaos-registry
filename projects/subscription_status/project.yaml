schema: adaos.project.v1
kind: project
id: subscription_status
version: 0.1.17
profiles:
- adaos.builder.component_project.v1
components:
  owned:
  - ref: skill:subscription_status_skill
    role: primary
    exposure: application
    lifecycle: bound
    relations:
    - uses
  dependencies: []
entrypoints:
- id: subscriptions
  presentation: skill:subscription_status_skill
  default: true
  bindings:
    surface: subscription_status
catalog:
  title: Subscriptions
  description: User-facing AdaOS subscription status widget and modal backed by the
    local economic entitlement snapshot with explicit Root refresh.
  categories:
  - system
  - account
  tags:
  - subscriptions
  - usage
  - cbs-provider
publication:
  stage: beta
  visibility: listed
  channel: stable
install:
  default: false
  features:
  - id: default
    title: subscription_status_skill
    default: true
    optional: false
    components:
    - skill:subscription_status_skill
compatibility:
  required_entrypoints:
  - subscriptions
  required_contracts:
  - capability:subscriptions.status.inspect@^1.0.0
  validation_profiles:
  - subscriptions.local
lifecycle:
  uninstall:
    components: remove_if_unreferenced
    runtime_data: retain
    source_artifacts: retain
created_at: '2026-09-02T17:23:11.121480Z'
created_by: builder:qualifier
permission_profile:
  schema: adaos.application.permission_profile.v1
  required:
  - id: workspace.write
    purpose: Refresh and persist the local subscription projection and explicit plan-change requests.
    approval_policy: grant_on_install
  optional: []
  secrets: []
  external_providers: []
  llm_model_use: []
  notifications: []
  background_actions: []
  data_practices:
    collected: [subscription_status, usage_summary, plan_change_request]
    linked_to_user: [subscription_status, plan_change_request]
    retention: The current local entitlement projection and explicit requests are retained.
    sent_off_device: []
    tracking: false
