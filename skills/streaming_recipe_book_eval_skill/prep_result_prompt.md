# Preparation Summary

- Skill-local durable storage is available through the AdaOS runtime data directory.
- The skill requires no credentials, network access, external APIs, or extra dependencies.
- DEV execution still requires the AdaOS orchestrator to prepare and activate version `1.0.0` after projecting the skill sources.
