# redevice_list

Workspace skill for managing ReDevice endpoint admissions.

It exposes a WebIO-backed dashboard that lists registered ReDevice sessions/endpoints,
shows diagnostics, endpoint manifest and endpoint policy, and provides revoke/retire
actions.

The canonical declarative UI file is `webui.json`. `webio.json` is kept as a small
compatibility alias for discovery.
