# Media Center Coordinator

`media_center_skill` is the logical Media Center coordinator. It owns the global catalog read model, work/variant/collection identity, profile-scoped favorites and resume state, bounded search, and playback planning. It does not walk filesystems or own source media.

`media_library_agent` owns node-local roots and asynchronous scans. The coordinator invokes its public tools and consumes ordered `adaos.media_library.source_delta.v1` pages. The agent registers each source through `adaos.sdk.io.media.register_media_file`; only a root-bound reference is stored in `.adaos`, while media bytes stay at their original path.

## Catalog

The catalog filter drawer is entirely UI-as-data. It reads profile-aware genre and year facets from `metadata_facets`, applies genre/year/minimum-rating filters on the server, and exposes the same bounded sort contract used by list, grid, and carousel layouts. The compact toolbar opens the generic left-side overlay without adding a permanent row of controls to TV surfaces. All labels remain in the skill-owned English and Russian dictionaries.

`library()` uses deterministic FTS over normalized title, filename, relative path, meaningful folder segments, tags, people, aliases, and collection names. Queries execute explicitly when the tool is called. FTS first selects a bounded 192-record window by default, then applies profile policy and the versioned `deterministic-fts-v2` lexical order to a narrow id/rank page before materializing full rows. `MEDIA_CENTER_SEARCH_CANDIDATE_LIMIT` can tune the window from 64 to 10,000; responses expose the effective limit, candidate count, and truncation signal, and never claim an exact total for a bounded search. Exact and rare terms still match against the complete FTS index before the window is applied.

`deep_search()` keeps FTS first, then adds a bounded local discovery stage with transliterated phonetic, trigram, and 48-dimension local text-embedding signals, followed by bounded agents for technical and not-yet-materialized shard fields. Every stage preserves profile policy, reports partial participants, and exposes ranking evidence. The trigram index admits at most 5,000 candidates by default (20,000 hard maximum) and fully scores the best 600 by default (5,000 hard maximum). Catalog pages use opaque versioned cursors and are clamped to 30 rows. Non-search continuation uses keyset anchors for stable latency as the catalog grows; bounded FTS continuation uses its opaque offset inside the fixed candidate window. Both paths fetch one extra row instead of performing an unbounded exact count, and `total_count_exact=false` exposes that truth. Legacy offset cursors remain readable for rolling upgrades. The compact player projection remains clamped to ten entries, while durable playback sessions can request a bounded queue of up to 500 entries. Images are excluded from the default `playable` view.

The coordinator derives explicit `MediaSource`, `MediaVariant`, `MediaWork`, `MediaCollection`, and membership rows. Deterministic grouping preserves series/season/episode, album/disc/track, audiobook/part/chapter, and source-folder levels. Episode filenames are parsed by the production-stable GuessIt library behind a bounded cache, with the previous deterministic parser as a fallback; series identity therefore does not depend on inconsistent season-folder labels. Dependency diagnostics are bounded to warnings even when the host runtime uses DEBUG logging, so a catalog migration cannot turn parser rule traces into an I/O workload. TMDB remains an optional observable metadata-claim provider and ambiguous external matches do not become authoritative grouping decisions. `collection_contents()` resolves membership at every hierarchy level, returns at most 30 profile-admitted items plus bounded child collections and breadcrumbs, and gives collection cards sanitized representative artwork. Opening a series, album, or audiobook is therefore a navigation operation; explicit Play All builds the durable collection queue. Folder navigation is a separate lazy, cursor-backed read model with breadcrumbs. Its first page contains only configured agent-owned roots; subsequent pages, breadcrumbs, and folder queues are scoped by both agent and root identity. Unstructured legacy Media Server rows remain searchable but cannot flatten the folder browser or collide across equal paths on different disks. The transactionally maintained folder-node projection contains paths, revisions, and counts only; direct files remain bounded catalog rows and media bytes are never copied. Profile policy is still checked against source rows before a folder is exposed. Duplicate results are review candidates only and never authorize source deletion. Metadata, merge, split, and regroup corrections are audited and reversible; alias activation/revocation remains explicit evidence.

`get_item()` exposes bounded metadata provenance, background operations, and
all available variants of the same work without exposing an absolute source
path. The public `library_path` is assembled from the sanitized relative folder
and file name. `update_item_metadata()` is the SDK-flat user/skill interface for
preferred title, original title, overview, year, genre, artist, album and series
claims and for rejecting an incorrect provider match. Preferred profile claims
win projection deterministically; provider rejection is field-aware, audited,
and reversed through the ordinary correction journal.

Provisional audio work identity includes normalized folder/collection context,
so files named only `0.mp3` or `01.mp3` in different books cannot become
playback alternatives by filename alone. Matching contextual tracks from
different agents may still become variants. A variant id belongs to its exact
physical source and remains stable when migration, enrichment, or a reviewed
correction reclassifies that source under another work.

Profile-owned playlists have revision-safe ordered membership and explicit `private`, `household`, or `shared` visibility. Playlist reads remain cursor-backed and catalog/player limits remain independent; deleting a playlist never deletes source media.

`add_playlist_item()` appends idempotently within the profile boundary, while
`play_on()` moves the current ordered source context to a selected online target
by creating or revision-safely updating a durable `media_control_skill`
session and then issuing one idempotent Play command. Media bytes still travel
from the selected source node to the playback endpoint, never through the
controller surface.

## Playback Planning

Collection item selection preserves the active series, season, album, audiobook, folder, or playlist as queue ownership and sends the selected item as an explicit start witness. The endpoint coordinator receives up to 500 entries, while the player selector renders only a ten-entry window around the active item. Next and Previous therefore traverse the complete context without loading a large dropdown into the DOM. The player modal also exposes the active Personal, Household, or Kids profile so favorites, history, resume state, and policy remain explicit at playback time.

Discovery uses Unicode-safe Cyrillic transliteration and a bounded local similarity signal. The coordinator owns a deterministic 48-dimension projection so its installation and memory budget never depend on a native ML stack. A learned embedding backend belongs behind a separately deployed enrichment service boundary and must publish its backend identity with each claim; heavy model dependencies are not loaded inside the coordinator process. Profile recommendations combine media kind, folder context, genres, and artists, then apply a diversity cap and return machine-readable reasons.

`playback_plan()` deterministically selects one available variant for the target endpoint. The decision considers an explicit user override, advertised codecs, maximum video height and bitrate, quality/language preferences, and source/endpoint co-location. Its result includes bounded decision evidence rather than hiding policy inside the client.

Every plan carries an `adaos.media_center.playback_route.v1` contract. A source-agent direct URL is preferred when available; the plan always describes a root-routed HTTP fallback tied to the source node. `ensure_rendition()` routes a browser-compatibility request to the exact source-owning agent. `rendition_operations()` exposes only a bounded compact coordinator projection of source-relative names, durable states, byte counters, and diagnostics; UI surfaces do not call an internal library-agent history tool or receive its full delivery descriptors. Completed derived resources are hidden variants of the same work and remain tied to the exact source revision/fingerprint. Node-local artwork is projected as `adaos.media.artwork.v1`; catalog responses expose a sanitized `adaos.media.resource.v1` descriptor beside the compatibility URL, bounded dimensions, provenance, state, and exact-source evidence. The app shell resolves that descriptor through the direct media channel or authenticated routed HTTP rather than treating a root-relative URL as a page-origin asset. Extraction paths and arbitrary descriptor metadata never cross the public catalog boundary. `build_playback_queue()` creates ordered snapshots from an item, work, collection, folder, profile-visible playlist, or the current filtered catalog page and preserves display facts such as byte size and modification time. The current-page source captures query, filters, sort, and cursor and is intentionally bounded to the same 30 rows the person was viewing; collection and folder queues retain their logical order up to 500 while the client renders only a ten-item window. It also returns the generic `adaos.playback.endpoint_control.v1` adapter descriptor that lets the app shell open and reconcile a durable endpoint session without embedding Media Center behavior in the client. Queue construction never copies original media bytes. Reference-only `media_server` catalog rows remain playable during migration: the queue normalizes their existing core media-resource URLs into the same public route contract without creating a local media copy.

Coordinator FTS rows share the owning `catalog_items.rowid`. Both lexical and
trigram indexes use FTS5 external-content tables, so SQLite does not retain a
second copy of either search corpus. A compact transliterated `fuzzy_text`
column preserves cross-script matching while the native trigram tokenizer
avoids the former hex-expanded token document. Catalog triggers update both
indexes by row id; schema migration normalizes the fuzzy text, rebuilds the
indexes before enabling those triggers, and never exposes a partially updated
index. Compact diagnostics use the ordinary catalog row count implied by this
one-to-one invariant; they never count-scan the FTS5 token payload.

Delta ingestion advances durable per-kind cursors instead of materializing work for the entire catalog. The coordinator keeps configurable active windows of 512 metadata, 256 fingerprint, and 256 embedding jobs, fills them in bounded 500-row passes, and resumes from persisted cursors as work completes. A newer source revision still cancels an older queued job for the same item and kind. Upgraded catalogs compact oversized legacy queues in 5,000-row batches without touching running work, completed claims, or source rows. Queue claim/recent/status reads have dedicated indexes; interrupted `running` jobs are retried after a bounded stale interval or failed after their retry budget. Terminal history retains the newest 10,000 receipts and is pruned in bounded 5,000-row maintenance batches, so repeated scans cannot grow the operational table indefinitely. One paced low-priority worker runs every eligible versioned provider, records bounded `MetadataClaim` rows with provenance/confidence, and publishes a compact replace-mode `media_center.operation_state` at no more than one update per two seconds. It does not rebuild home shelves after each job. A single full library snapshot is published on the `busy -> idle` transition so collection state converges from updating to ready. The built-in deterministic provider uses only indexed filename, folder, tag, and technical evidence. Local Kodi/Jellyfin-compatible NFO sidecars are parsed by the source-owning agent with a bounded witness and take precedence over inferred and external claims without modifying the media folder. The coordinator transactionally materializes selected claims into an indexed metadata projection and normalized facets; catalog reads can filter by genre, year, rating, and content rating and sort by title, dates, ratings, duration, progress, plays, resolution, bitrate, or a bounded random order without scanning JSON in the browser. User-preferred claims outrank NFO, NFO outranks external providers, and all output carries field-level provenance.

External enrichment is managed in Media Center Settings and enabled by default. MusicBrainz needs no credential; the source-owning agent first extracts embedded title, artist, album, date, genre, track/disc, and MusicBrainz identifiers so numbered audio files can be matched without leaking their paths. Its public endpoint is paced to one request per second and returns recording/release identity plus Cover Art Archive candidates. Repeated transport/TLS failures open an exponential bounded circuit breaker, publish `retry_after_seconds`, and become provider warnings with stable skill-owned diagnostic codes such as `musicbrainz_tls_handshake_failed`; deterministic local enrichment still completes instead of turning every item into a failed job or hammering the upstream. TMDb requires the user's free API Key (v3 auth) or API Read Access Token. The Settings flow links to TMDb's official API page, validates the credential before storing it in the skill secret store, and never returns the value to UI data. A successfully read or saved credential is retained in the worker process, so a transient secret-store outage reports a degraded provider instead of the false `credentials_missing` state. TMDb receives only normalized title/year/kind or a locally supplied TMDb id, selects a deterministic multilingual match, then reads bounded details, credits, ratings, trailers, external ids, and artwork candidates. Both providers cache requests and expose counters, readiness, and failures through operation status; no source path or descriptor is sent. Updating provider settings advances a provider revision and coalesces queued metadata jobs to one current job per item. Technical probes, exact/perceptual fingerprints, thumbnails, and embeddings use the same provider/job boundary; unavailable providers remain observable without blocking catalog reads or touching source bytes. Perceptual duplicate groups are review-only evidence: no candidate is automatically merged or deleted.

SQLite WAL mode is established during schema creation and migration, not renegotiated on every read connection. Read connections retain foreign-key enforcement, a 30-second busy timeout, and `synchronous=NORMAL`; this avoids WAL mode lock coordination in catalog, playback, and search hot paths while continuous agent deltas are being committed. The coordinator persists source routing facts in typed columns and keeps only the descriptor remainder; the source agent remains authoritative for the complete descriptor. Metadata projection stores only resolved claim deltas over compact source metadata, excludes internal embedding vectors, and reconstructs the public view at read time. A base `MediaVariant` reuses that catalog descriptor, while derived renditions retain their own exact-source-bound descriptors.

Large-catalog JSON normalization is a resumable `catalog -> projection` maintenance pass owned by the enrichment worker. It commits at most 1000 rows per step, publishes its cursor/counters with enrichment status, and is not part of slot activation. The `compact_storage` tool advances the same checkpoint under explicit batch and wall-clock budgets, matching the node-agent maintenance contract even when startup has not launched enrichment. New catalogs use incremental auto-vacuum. Operation and status responses publish allocated/reclaimable bytes and logical-compaction state. Once logical compaction is complete, `optimize_storage(reclaim=true)` raises a durable database maintenance fence observed by sync and enrichment workers in every process, checkpoints WAL, verifies temporary disk headroom, performs the deliberate SQLite `VACUUM` that converts an upgraded catalog to incremental auto-vacuum, clears the fence, and restarts runtime bootstrap. Startup never hides that potentially long, disk-intensive physical operation inside activation.

Agent availability is independent from known catalog identity. The coordinator discovers ready `skill:media_library_agent` service instances through `adaos.sdk.distributed`, invokes each exact instance through the public service boundary, and stores one cursor per instance/agent binding. Reads retain known rows but report `partial=true` when an expected instance is stale, missing, or unavailable. Every applied source revision advances a catalog revision, and replayed agent deltas are ignored idempotently.

The colocated compatibility path first reads the agent's stable identity and then resumes that agent's durable cursor. It never restarts delta ingestion at sequence zero merely because distributed topology is not configured, and it never borrows a cursor from a different prior local agent.

When an agent delta names the same contained source through an exact path or a root/path alias, the coordinator retires the legacy `media_server` compatibility row from normal reads. Favorite, resume/history, play count, rating, hidden state, and playlist membership are merged into the canonical agent row before retirement. This removes stale externally matched artwork and duplicate UI entries without deleting the original file or its core media reference.

MusicBrainz projection consumes both recording `genres` and ranked community
`tags`, deduplicates them deterministically, and requeues previously completed
audio metadata jobs that produced no genre facet under the older recipe.
External poster, cover, and backdrop candidates remain provenance-bearing
metadata claims. The enrichment worker downloads at most one usable candidate
per artwork kind into the bounded node cache, validates its image signature and
size, and registers it as an AdaOS media reference. UI surfaces therefore fetch
external artwork through the authenticated media transport instead of page-origin
HTTP. Cache counters and stable failures are published in Metadata activity.

## Personal State

Favorites, recent playback, resume positions, completion, ratings, hidden items, playlists, and recommendations are keyed by `profile_id`. Catalog queries and playback plans enforce media-kind, maturity, explicit-content, hidden-item, and shared-screen history policy. Revisioned endpoint observations from `media_control_skill` are folded into the same profile checkpoint model, so Recent and resume state converge even when playback is controlled from another browser. Observation buckets are bounded and terminal transitions remain observable without streaming high-frequency positions through UI state. Every profile has one durable monotonic projection revision; changing a second item therefore advances the stream even when another item's row revision is already larger. Mutations publish the bounded replace-mode `media_center.library_state` stream with catalog and personal revisions, participation, home shelves, and an explicit collection state. Its sequence is the sum of the independently monotonic catalog and personal revisions, so either plane always advances the replacement variable. Background progress is a separate `media_center.operation_state` stream, preventing metadata throughput from forcing repeated catalog projections. The state machine distinguishes loading, indexing, unconfigured, configured-but-empty, ready, updating, and unavailable from persisted coordinator and agent witnesses; an absent snapshot is never interpreted as an empty library. Multiple browsers can therefore converge without putting a full catalog into synchronized state. Both streams support snapshot replay after reconnect. Every library snapshot echoes the subscribed `profile_id` and `shared_surface` values in stream metadata and uses a distinct variable identity, so personal and shared-screen projections cannot cross-deliver. Home shelves are materialized once per catalog/profile revision into a bounded process cache; snapshot requests reuse that ready projection while refreshing lightweight collection progress. Collection pages select their bounded title page before counting memberships or resolving representative artwork, so reconnecting browsers do not serialize full-library collection scans.

The Home stream is a presentation projection, not a metadata transport. Its cards retain navigation identity, the displayed quality fact, and a minimal direct/root-routed artwork descriptor; provenance, personal history details, and source diagnostics remain available through bounded catalog and detail tools. This keeps cold-start fanout below the stream-owner pressure limit without weakening media routing or metadata ownership.

Recommendations evaluate at most three catalog pages, use only local favorite and history signals, include bounded machine-readable reasons, and can be disabled per profile. Profiles also own home-row order, default list/grid/rail view, density, and preferred playback target. `voice_request` is the structured entrypoint for search, collections, favorites, status, target discovery, playback, and transport control. It returns at most ten visual candidates, asks for clarification when media or targets are ambiguous, and delegates sessions and commands to `media_control_skill` so voice and direct controls share policy, leases, and revisions. A natural multi-step request is translated into a bounded `media.compound_control` workflow request with profile/target context, per-step idempotency and schedule constraints. It is never executed directly: the governed workflow owner must obtain confirmation and reconcile unknown effects.

`diagnostic_export()` combines bounded deployment, topology, scan, catalog/search, provider, playback, route, QoE, and optional browser observations. Paths, credentials, direct URLs, media bytes, and unbounded logs are excluded or redacted. Repair recommendations contain review-required tool plans; they never mutate deployment or data automatically.

Topology administration remains reviewed and SDK-only. `define_topology()` binds a service definition to the current desired ProjectRelease digest, verifies the definition/group identity and all optimistic revisions before any topology write, and rejects a component package digest as a release identity. `plan_topology_change()` persists an immutable dry-run plan, `apply_topology_change()` reads that plan through the SDK and applies its exact digest with idempotency and only the approvals declared by the plan, `topology_operation_status()` reads one durable operation by ID, and `handoff_authority()` performs an explicit revision- and epoch-fenced recovery handoff. Topology application has a 10-minute tool budget for large catalog snapshots; the durable core operation remains the source of truth and can resume an interrupted `pending` or `running` plan with the same idempotency key. Project deployment uses `adaos.sdk.deployment.submit`, returns the durable operation immediately, and exports `deployment_operation_status` for direct bounded progress reads; rollout duration is not coupled to an interactive tool timeout. The coordinator never imports the distributed runtime store or adapter implementation.

Compatible rolling upgrades use `adaos.distributed.service_definition.v2`. Before deployment, the reviewed definition names the new desired ProjectRelease and includes the current exact release in its bounded overlap list. Media Center reads the previous definition through `adaos.sdk.distributed.get_service_definition` and rejects a missing overlap before any topology mutation. `topology_status()` exposes the versioned definition and overlap for operator review. After the batch-one Project rollout reports every live instance on the new release, a later definition revision may remove the old digest; core remains the final admission authority throughout.

Distributed catalog catch-up is cursor-based and lifecycle-managed. The coordinator adapts an oversized agent page down to the core service-invocation envelope, keeps the accepted cursor unchanged while retrying, and continues incomplete catch-up in one single-flight background worker. Ordinary reads ensure the worker exists without waking it, and an unchanged idle poll does not republish the full Home projection. Catalog-change events explicitly wake the worker. The worker inherits the admitted skill execution context with `contextvars.copy_context()`, so SDK capability checks remain active off the invocation thread. `dispose()` stops both the sync and enrichment workers; UI reads only bootstrap synchronously when the coordinator catalog is empty.

Runtime bootstrap and schema admission are single-flight. Catalog and coordinator revision reads retry bounded SQLite lock contention, but an unavailable revision is never interpreted as permission to migrate. Agent change notifications received before bootstrap completion only wake the process-owned bootstrap; they do not race it with another repository or migration. Enrichment records transient repository failures and retries without terminating its worker thread.

Playback observations in `loading` or `buffering` state do not create history. Fifteen-second resume checkpoints remain durable, while Home projection publication is coalesced to state transitions, terminal observations, and one-minute position buckets. This keeps a playing item current without rerendering every rail on every endpoint tick.

## Compatibility

The local public-tool invocation and old Media Server discovery methods remain bounded compatibility fallbacks when no distributed agent instance is registered or the handler is exercised outside an AdaOS skill context. In a Project deployment, exact service-instance invocation is authoritative; root and scan commands return asynchronous job identifiers.

Project closure references one published workspace revision for the scenario,
coordinator, control plane, and agent. Process-local state is excluded from both
that source boundary and package construction; a corrected closure receives a
new Project version instead of replacing an immutable release digest.

## User-Facing Errors

Tools return stable machine codes and may include `human_message_i18n`. Media Center error and UI translations live in this skill's `assets/i18n/*.json` resources, not in core, the scenario, or the client. The UI-as-data scenario references those keys and retains English fallbacks. Core browser-asset publication turns the dictionaries into immutable content-addressed URLs during materialization. `human_message` remains a fallback for clients that have not loaded the runtime dictionary.
